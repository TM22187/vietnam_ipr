# Vietnam LPR dependencies.
# After installing in a virtual environment, optionally run:
# pip freeze > requirements-lock.txt
#
# lpr_pipeline.py uses the PaddleOCR 3.x API (.predict, rec_texts).
# PaddleOCR 2.x requires changes in read_plate_text().

# Detection and training
ultralytics>=8.0,<9       # YOLOv8, includes torch when it is not already installed
paddleocr>=3.0.0          # OCR engine for reading license plate text
paddlepaddle>=2.5.0       # PaddleOCR backend

# Image processing and utilities
opencv-python>=4.8        # Read images/video and draw bounding boxes
numpy>=1.24               # Numeric arrays
pillow>=9.0               # PIL image support for notebooks and pipeline helpers
pyyaml>=6.0               # Read YAML config files
matplotlib>=3.7           # Display images and charts in notebooks
