# Vietnam LPR — dùng venv; sau khi cài xong: pip freeze > requirements-lock.txt
# lpr_pipeline dùng API PaddleOCR 3.x (.predict, rec_texts). Nếu dùng 2.x thì cần sửa hàm read_plate_text.

# ── Phát hiện & huấn luyện ──────────────────────────────────
ultralytics>=8.0,<9       # YOLOv8 (bao gồm cả torch khi chưa có)
paddleocr>=3.0.0          # PaddleOCR đọc ký tự biển số
paddlepaddle>=2.5.0       # Backend của PaddleOCR

# ── Xử lý ảnh & hỗ trợ ─────────────────────────────────────
opencv-python>=4.8        # Đọc ảnh, video, vẽ bounding box
numpy>=1.24               # Tính toán mảng số
pillow>=9.0               # Đọc/ghi ảnh (PIL) - dùng trong notebook & pipeline
pyyaml>=6.0               # Đọc file config .yaml (data.yaml, roi.yaml)
matplotlib>=3.7           # Hiển thị ảnh và biểu đồ trong notebook
pyinstaller>=6.0          # Đóng gói app desktop thành file .exe trên Windows
